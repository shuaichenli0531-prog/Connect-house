export default function EditableHint({ text }) {
  return (
    <div className="mt-3 rounded-xl border border-gold/30 bg-gold/10 px-4 py-3 text-xs uppercase tracking-[0.25em] text-gold">
      {text}
    </div>
  );
}
